# Useful commands
```powershell
composer install
php artisan migrate
php artisan serve
php artisan queue:work --queue=ai,default --tries=3
php artisan telegram:set-webhook https://example.com/api/telegram/webhook
vendor\bin\pint
php artisan test --compact
```
Configuration is documented in README.md and .env.example.